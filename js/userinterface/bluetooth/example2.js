window["bluetooth_example2_conf_options"] = {

    // Only devices whose BLE name starts with one of these prefixes will appear in the browser device picker.
    filterDevices: [
        {namePrefix: 'Sensor'},
        {namePrefix: 'sensor'},
        {namePrefix: 'SENSOR'}
    ]
};